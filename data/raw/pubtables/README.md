# PubTables-1M local data

The full PubTables-1M dataset is intentionally not bundled with this repository.
Use `scripts/prepare_pubtables_subset.py` after obtaining the official archives from
the Microsoft Table Transformer project or its Hugging Face dataset mirror.
The script creates a deterministic evaluation subset and a file-level manifest.
